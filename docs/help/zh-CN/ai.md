---
title: AI 功能
category: AI
order: 80
summary: 可选、不绑定任何厂商，而且句句扎根在你真实的代码上。
keywords: ai openai anthropic ollama local 本地 llm 大模型 commit message 提交消息 explain 解释 review 评审 wiki 有依据 grounded
---

# AI 功能

每一项 AI 功能都是**可选的**，在你配置好一个服务商之前它们全是关着的。在你明确
提出某个请求之前，什么都不会被发到任何地方去。

![AI 设置](../../screenshots/settings-ai.webp)

## 服务商

内置了 **OpenAI、Anthropic、OpenRouter、Groq、Mistral 和 Ollama**（完全本地）的
预设，也支持任何兼容 OpenAI 的接口。模型列表是实时拉取的，你还可以加自定义指令。

> 只有 OpenAI 是经过充分实战检验的。其余几家用的是兼容 OpenAI 的调用格式，理论上
> 应该能用——但没有验证过。

## 它能做什么

| 功能 | 你会得到什么 |
|---|---|
| **提交消息** | 依据你已暂存的差异，按你选定的风格生成摘要（以及可选的正文） |
| **解释这个文件** | 在侧边面板里用大白话讲解——普通、简洁、ELI5……甚至还有海盗腔 |
| **悬停即解释** | 按住 <kbd>⇧</kbd> 指向一个标识符，得到一行解释，外加它所依据的那些行 |
| **冲突解决** | 把一份合并方案提议到可编辑的输出里——绝不自动套用 |
| **PR 评审** | 概括一份差异并指出风险，每一条都锚定到真实的 `path:line` |
| **PR 描述** · **分支名** | 依据该分支的提交和差异起草 |
| **主题** · **提交图配色** | 由一段提示词生成 |
| **智能暂存** | 建议哪些东西该进这一次提交 |

## 有依据，不是瞎猜

评审看到的差异是**带标号的变更块**，而且它只被允许引用这些标号；随后由 Gitcito
把每个标号还原成真实的文件和行号。凭空编造位置的模型会被**驳回并重新提问**，
所以结论指向的永远是真实存在的代码。

悬停即解释只读取标识符周围一扇带行号的窗口——在差异里，只读屏幕上可见的那些变更
块——所以当某个定义在别处时，它会直说，而不是编一个出来。答案按文件版本缓存。

**被遮蔽的密钥文件绝不会被发送。** 密钥遮蔽规则覆盖到的文件同样不会。

**另请参阅：** [仓库百科](repo-wiki.md) · [安全与机密](security.md)
