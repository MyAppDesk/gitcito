---
title: 스택 브랜치
category: 브랜치와 수술
order: 43
summary: 서로 의존하는 브랜치의 사슬, 그리고 연쇄 재적재.
keywords: 스택 stack stacked branches 스택 브랜치 graphite restack 재적재 의존 dependent 사슬 chain 부모 parent PR 단계별 per level
---

# 스택 브랜치

스택은 각 브랜치가 아래 브랜치 위에 쌓이는 사슬이에요. `main → api → ui` 처럼요.
작은 PR 세 개를 리뷰하는 편이 거대한 PR 하나를 리뷰하는 것보다 나아요.

![브랜치 스택](../../screenshots/branch-stack.webp)

Gitcito는 스택을 아래에서 위로 보여 주면서 각 단계의 커밋 수를 함께 표시하고,
**단계마다 PR을 열 수** 있게 해 줘요. 각 PR은 `main` 이 아니라 자기 부모를
대상으로 삼아요.

## 재적재

아래쪽 브랜치가 바뀌면 — 예를 들어 `api` 의 리뷰 의견을 반영했다면 — 그 위의 모든
브랜치는 이제 잘못된 기반 위에 놓여 있어요. **재적재**는 `rebase --onto` 로 사슬
전체를 연쇄적으로 리베이스해서, 부모를 다시 쓴 결과가 자식들에 커밋을 중복시키지
않도록 해요.

## 연결 정보가 있는 곳

부모 연결은 **git 설정**에 저장돼요. 그래서 저장소와 함께 이동하고 다시 클론해도
살아남아요. 어떤 서비스에도 아무것도 남지 않아요.

**함께 보기:** [대화형 리베이스](rebase.md) · [호스팅과 풀 리퀘스트](hosting.md)
