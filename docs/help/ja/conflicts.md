---
title: コンフリクトの解決
category: 変更を扱う
order: 32
summary: どちらがどちら側なのかを教えてくれる、3 ペインのリゾルバ。
keywords: コンフリクト conflict リゾルバ resolver マージコンフリクト merge conflicts ours theirs 解決 resolve マーカー markers 3 way rerere 記録した解決の再利用 reuse recorded resolution 記憶 remember 再生 replay
---

# コンフリクトの解決

マージ、リベース、チェリーピック、リバートが止まったとき、バナーが **何が** 止まった
のか、**何と何の間で** 止まったのかを伝えます。単なる「コンフリクト」ではなく、
「`feature/x` を `main` にマージ中」と表示します。

![コンフリクトリゾルバ](../../screenshots/conflict-resolver.webp)

## なぜここが衝突するのか

ヘッダーの **なぜここが衝突するのか** は、ブランチが分かれてからこのファイルに触れた
コミットを、側ごとに一覧します。中身は `git log --merge` — git がずっと前から積んで
いるのに、誰も見つけないコマンドです。

![衝突したファイルに触れた、それぞれの側のコミット](../../screenshots/conflict-why.webp)

マーカーが言うのは *何が* ぶつかっているかです。こちらが言うのは *誰が、なぜ* 変えた
のかで、たいてい解決を決めるのはそちらです。何も出てこなければ、どちらの側もこの
パスそのものへの変更をコミットしていません。衝突は名前の変更か移動から来ています。

## 3 つのペイン

| ペイン | 中身 |
|---|---|
| 左 | **こちら側（ours）** — いたほうの側。そのコミットのラベル付き |
| 右 | **あちら側（theirs）** — 入ってくる側。そのコミットのラベル付き |
| 中央 | **出力** — 編集でき、行番号があり、実際にステージされるもの |

3 つのペインはいずれもサイズを変えられます。

## 選ぶ

**行** 単位、**チャンク** 単位、あるいは **片側まるごと** を一度に。答えが「両方
残す」のときは、チャンクの両側を採用することもできます。コンフリクトを 1 つずつ辿る
ナビゲータが残りを案内するので、マーカーを置き去りにしてしまうことはありません。

## AI の補助

AI を有効にしていると、**AI で解決** が出力ペインにマージ案を提示します。自分から
何かを適用することは決してありません。読んで、直して、ステージするのはあなたです。
[AI 機能](ai.md) を参照してください。

## そもそも起こさない

[コンフリクトレーダー](conflict-radar.md) は、マージする前に、どのブランチが衝突する
のかを教えてくれます。

## git に覚えさせる（rerere）

長く生きているブランチをリベースすると、毎回同じコンフリクトに出会います。`rerere`
— *reuse recorded resolution*（記録した解決の再利用）— が git の答えです。コンフリクトを
どう決着させたかを覚えておき、まったく同じものが次に現れたときにその答えを再生します。

**設定 → 一般 → コンフリクトの解決を記憶する。** グローバルの git 設定に
`rerere.enabled` を書き込むので、コマンドラインでも同じように振る舞います。

git があなたの代わりに答えたとき、リゾルバは「コンフリクトマーカーがありません」という
空の画面を見せる代わりにその旨を伝え、**この解決を忘れる** を提示します。これは記憶を
捨てると *同時に* コンフリクトを呼び戻すので、違う決着をつけられます。

知っておくとよいことが 2 つあります。

- **再生された解決はステージされません** — *再生された解決を自動でステージする* を
  有効にしない限りは。オフのままにしておいてください。ここで一度止まる意味は、
  記憶された答えがこのマージでは間違っているかもしれない、という点にあります。見ずに
  ステージすることが、それをコミットまで運んでしまう経路です。

  だから再生されたファイルは **コンフリクトしたファイル** に残り続けます。git は中身を
  書いたものの、インデックスは未マージのまま抱えており、それを片付けるのはステージ
  だけだからです。動かすのは、リゾルバの **このままステージ**、または一覧の
  **すべて解決済みにする** です。
- **rerere はすべてのコンフリクトを理解するわけではありません。** 追加／追加や
  削除／変更のコンフリクトには前像が残らないので、毎回そのまま戻ってきます。設定に
  出ている件数が実際に抱えている数で、**すべて忘れる** で空にできます。

**関連項目:** [コンフリクトレーダー](conflict-radar.md) · [マージとリベース](merging.md)
