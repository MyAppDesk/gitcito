---
title: ワークツリーとサブモジュール
category: 同期と複数リポジトリ
order: 54
summary: 1 つのリポジトリの複数のチェックアウトと、リポジトリの中のリポジトリ。
keywords: ワークツリー worktree worktrees サブモジュール submodule submodules リンク linked チェックアウト checkout init sync 同期
---

# ワークツリーとサブモジュール

## ワークツリー

ワークツリーとは、同じリポジトリの 2 つ目のチェックアウトを、それ専用のフォルダに
置いたものです。だから `feature/x` を置いたそのままの状態に保ちながら、`main` を
覗きに行けます。スタッシュは要りません。

- サイドバーからワークツリーを作成・削除でき、**別ウィンドウ** で開けます。
- ローカルブランチを右クリック → **ワークツリーで開く** で、隣のフォルダに 1 つ作り、
  タブとして開きます。

![サイドバーのワークツリーとサブモジュールの欄。どちらも中身が入っている](../../screenshots/worktrees.webp)

## サブモジュール

サブモジュールの追加、更新（init とチェックアウト）、URL の同期、削除ができ、それぞれの
状態がその場でわかります。

| 状態 | 意味 |
|---|---|
| **同期済み** | 親が記録しているコミットでチェックアウトされている |
| **変更あり** | 別の場所でチェックアウトされているか、作業ツリーが汚れている |
| **未初期化** | 記録はあるが、一度もチェックアウトされていない |

![状態を持ったサブモジュールが 1 行ずつ並ぶ](../../screenshots/submodule-states.webp)

**関連項目:** [LFS とスパースチェックアウト](lfs-sparse.md) · [フェッチ、プル、プッシュ](syncing.md)
