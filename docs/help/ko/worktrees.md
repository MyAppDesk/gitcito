---
title: 워크트리와 서브모듈
category: 동기화와 여러 저장소
order: 54
summary: 한 저장소의 여러 체크아웃, 그리고 저장소 안의 저장소.
keywords: 워크트리 서브모듈 연결된체크아웃 초기화 동기화 worktree worktrees submodule submodules linked checkout init sync
---

# 워크트리와 서브모듈

## 워크트리

워크트리는 같은 저장소를 자기만의 폴더에 두 번째로 체크아웃한 것이에요. 그래서
`feature/x`를 떠날 때 그대로 둔 채로 `main`을 들여다볼 수 있어요. 스태시할 필요
없이요.

- 사이드바에서 워크트리를 만들고 지울 수 있고, **자기만의 창**으로 열 수도 있어요.
- 로컬 브랜치를 오른쪽 클릭 → **워크트리로 열기**를 하면 형제 폴더에 하나를 만들어
  탭으로 열어 줘요.

![사이드바의 워크트리 섹션과 서브모듈 섹션이 모두 채워진 모습](../../screenshots/worktrees.webp)

## 서브모듈

서브모듈을 추가하고, 업데이트(초기화 및 체크아웃)하고, URL을 동기화하고, 제거할 수
있어요. 각각의 상태도 실시간으로 함께 보여 줘요.

| 상태 | 의미 |
|---|---|
| **동기화됨** | 부모가 기록한 커밋에 체크아웃되어 있어요 |
| **수정됨** | 다른 곳에 체크아웃되어 있거나, 변경 사항이 있어요 |
| **초기화되지 않음** | 기록은 되어 있지만 한 번도 체크아웃되지 않았어요 |

![서브모듈이 한 줄에 하나씩, 각자의 상태를 달고 있는 모습](../../screenshots/submodule-states.webp)

**함께 보기:** [LFS와 sparse-checkout](lfs-sparse.md) · [페치, 풀, 푸시](syncing.md)
