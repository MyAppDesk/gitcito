---
title: 任意のコミットを編集
category: ブランチと手術
order: 46
summary: 過去のコミットのファイルやメッセージをその場で書き換え — カスケードを先にプレビュー。
keywords: edit commit コミット編集 rewrite history 履歴の書き換え amend 過去 past reword タイポ修正 fix typo cascade カスケード replay リプレイ rebase リベース in place その場 surgery 手術
---

# 任意のコミットを編集

タイポは 3 週間前のコミットの中にあります。いつもの直し方は対話的リベースです。
そのコミットで止め、編集し、続行し、祈る。Gitcito の直し方はこうです。コミットを
右クリック、**このコミットを編集**、テキストを変更、完了。コミット詳細パネルの
ペンボタンからも同じエディターが開きます。

![過去のコミットを編集しているところ](../../screenshots/commit-edit.webp)

## 何をするのか

`HEAD` まで直線でつながる任意のコミットを選びます。モーダルにそのファイルとメッ
セージが表示され、どちらも編集できます。そこから起きることは 2 つです。

1. **カスケードのプレビュー** は、編集したコミットより上のすべてのコミットを
   *メモリ上で* リプレイします（`merge-tree` チェリーピックの連鎖 — チェックアウ
   トなし、作業ツリーなし、ref なし）。子孫のコミットはそれぞれ緑か赤で表示される
   ので、編集がきれいに波及するのか後の変更と衝突するのかが、**何かが動く前に**
   わかります。
2. **履歴を書き換え** は本番です。同じ連鎖をプラミングで構築し、ブランチを
   `reset --keep` で移動します — コミットしていない変更は引き継がれるか、リセット
   が中止されて何も起きなかったことになります。先に
   [ガードスナップショット](recovery.md)が取られ、取り消しで元の連鎖が復元されま
   す。

リプレイされたすべてのコミットの作者情報と日付は保持されます。変わるのはハッシュ
だけです — それが履歴を書き換えるということです。

## カスケードが衝突するとき

後のコミットが、いま編集しているのと同じ行に触れていた場合です。プレビューはその
コミットを衝突ファイルとともに赤で示し、書き換えは実行を拒否します — 中途半端に
適用されることは決してありません。編集の内容を変えるか、
[対話的リベース](rebase.md)で衝突に正面から向き合ってください。

## 制限

- **直線の履歴のみ。** コミットと `HEAD` の間にマージがあると編集は無効になりま
  す — マージのリプレイは別の、より難しい問題です。
- バイナリファイルと 2 MB を超えるファイルは表示されますが編集できません。
- すでにリモートにあるコミットも編集できますが、次のプッシュは **強制プッシュ**
  にならざるを得ません — その決断をする前にモーダルが警告します。
- コミット内で削除されたファイルは編集できません（編集する内容がありません）。

**関連項目:** [対話的リベース](rebase.md) · [復旧と reflog](recovery.md) · [アブソーブ](absorb.md)
